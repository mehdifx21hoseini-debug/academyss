# Phase 0 — نتایج

**اولین اعداد واقعی این پروژه.**
ترمینال Build 6090 · نماد `US30CASH` · ۲۰۲۶-۰۸-۲۹

---

## Preflight — دور اول

**VERDICT: GO** · ۱۸ مورد OK · ۲ محدودیت · **صفر Blocker**

| سنجه | عدد واقعی | معنی |
|---|---|---|
| Terminal build | 6090 | خیلی جلوتر از حداقل ۱۷۳۰ |
| Max bars in chart | ۱۰۰٬۰۰۰٬۰۰۰ | سقف ترمینال مسئله نیست |
| حافظه‌ی آزاد | ۱۵٬۵۷۶ MB | |
| دیسک آزاد | ۶٬۷۱۷ MB | |
| مشخصات نماد | digits=2 · point=0.01 · tick_value=0.01 · tick_size=0.01 | Sizing از روی خود ابزار، نه فرمول Pip |
| جلسات اعلام‌شده | موجود | تشخیص شکاف از خود نماد خوانده می‌شود |
| تاریخچه‌ی M1 بارگذاری‌شده | ۲۰٬۰۰۰ کندل · ۱۳.۹ روز | ← این عدد **معتبر نبود**، پایین |
| **تاریخچه‌ی تیک** | **۲۱۱٬۷۲۲ تیک در ۳ روز** | **Fidelity کامل در دسترس است** |
| **نوشتن Custom Symbol** | **۳۵٬۱۵۹ کندل/ثانیه** | Warmup کامل D1 = **۸ ثانیه** |
| مشتق شدن تایم‌فریم بالاتر | ۱۰۰ کندل M5 از ۵۰۰ کندل M1 | **ادعای اصلی معماری، تأیید شد** |
| افزودن تیک سفارشی | ۱۰۰ از ۱۰۰ پذیرفته | |
| باز کردن چارت | موفق | |
| نوشتن فایل · Global Variable | موفق | مسیرهای Session و Integration کار می‌کنند |

---

## دو یافته‌ی مهم

### 🟢 Fidelity کامل در دسترس است

۲۱۱٬۷۲۲ تیک واقعی در سه روز. یعنی **مسئله‌ی R11** — ابهام SL/TP وقتی یک کندل هر دو را در بر می‌گیرد — برای داده‌ی اخیر **اصلاً پیش نمی‌آید**، چون ترتیب قیمت‌ها داده است نه فرض.

ماشین‌آلاتِ صداقتی که در Phase 9 و 10 ساخته شد سر جایش می‌ماند — برای وقتی که کاربر به گذشته‌ی دورتر می‌رود و تیک واقعی تمام می‌شود.

### 🟢 Warmup ارزان است

تخمین Phase 6 روی حدس ۶٬۰۰۰ کندل/ثانیه بنا شده بود. اندازه‌گیری واقعی: **۳۵٬۱۵۹**.

۲۸۸٬۰۰۰ کندلی که ۲۰۰ کندل روزانه لازم دارد، **۸ ثانیه** طول می‌کشد نه ۴۸ ثانیه. عدد پیش‌فرض عمداً محافظه‌کار نگه داشته شد (کاربری که «۴۸ ثانیه» شنیده و ۸ ثانیه صبر کند خوشحال می‌شود؛ برعکسش نه) و موتور بعد از اولین Seed خودش جایگزینش می‌کند.

---

## دو باگ که Preflight پیدا کرد — هر دو در خودِ Preflight

### 🔴 عددی که «همه‌چیز را تعیین می‌کند» را خودم سقف زده بودم

Preflight گفت «۲۰٬۰۰۰ کندل» — و دقیقاً ۲۰٬۰۰۰ خواسته بود. **سقف مالِ من بود نه بروکر.** بخشی که خودم اسمش را گذاشتم «عددی که همه‌چیز را تعیین می‌کند»، نمی‌توانست بین «بروکر ۲۰ هزار دارد» و «بروکر دو میلیون دارد» فرق بگذارد.

حالا `SeriesInfoInteger` می‌پرسد که ترمینال واقعاً چقدر دارد، و `SERIES_SERVER_FIRSTDATE` جواب سؤال مهم‌تر را می‌دهد: **آیا بیشترش هست؟**

### 🟠 خطای ۵۳۰۶ در پاک‌سازی

نمادی که در Market Watch انتخاب شده باشد حذف نمی‌شود. Preflight نماد را Select می‌کرد و هرگز رها نمی‌کرد.

**محصول این را از اول درست داشت** — `CSSRCustomSymbolManager::Destroy()` چارت‌ها را می‌بندد، مکث می‌کند، `SymbolSelect(false)` می‌زند و بعد حذف می‌کند. اسکریپت QA بود که نداشت.

> اینکه ابزارِ سنجش، باگی داشته باشد که خودِ محصول ندارد، خوشایند نیست ولی نتیجه‌اش سالم است: هر دو حالا یک ترتیب را دنبال می‌کنند.

---

## Spike Testهای Phase 0

هنوز اجرا نشده‌اند. Preflight زیرمجموعه‌ی فشرده‌ی A1/A2/D1 است و همه‌شان را سبز رد کرد، ولی اعداد کاملِ ۱۵ Spike هنوز ثبت نشده.

---

# اجرای دوم — نماد US30.U26 (۲۰۲۶.۰۸.۲۹، ۰۷:۴۵)

اجرای دوم روی یک نماد **کاملاً متفاوت** انجام شد، و همین ارزشش بود.

| | US30CASH (اجرای اول) | US30.U26 (اجرای دوم) |
|---|---|---|
| digits | ۲ | **۰** |
| point | ۰٫۰۱ | **۱** |
| tick_value | ۰٫۰۱ | **۱۰** |
| tick_size | ۰٫۰۱ | **۱** |
| ticks در ۳ روز | ۲۱۱٬۷۲۲ | ۱۹۰٬۱۴۴ |

**نتیجه‌ی مهم:** یک نماد سود هر واحد حرکت را ۰٫۰۱ حساب می‌کند و دیگری ۱۰ — یعنی
**هزار برابر**. اگر جایی از محصول فرض کرده بود «هر Point یک مقدار ثابت می‌ارزد»،
اینجا لو می‌رفت. نرفت: تمام محاسبات پولی از `CSSRRiskEngine` رد می‌شوند و آن
هم `tick_value / tick_size` می‌خواند، نه `point`:

```
double ticks = MathAbs(price_distance) / m_tick_size;
return ticks * m_tick_value * volume;
```

این همان چیزی است که «ممنوعیت Hardcoded Broker Logic» از اول برایش گذاشته شده
بود، و این اجرا اولین باری است که آزموده شد.

یک خط تازه به Preflight اضافه شد که وقتی `point != tick_size` است صریحاً
بگوید، چون دقیقاً همان‌جاست که کدِ ساده‌انگار اشتباه می‌کند — و به اندازه‌ی
نسبت آن دو اشتباه می‌کند، نه کمتر.

---

## باگ سوم که Preflight پیدا کرد — باز هم در خودِ Preflight

### 🔴 ادعای Performance بدون Measurement — از خودم

دو اجرا، دو عدد، از **یک نمونه‌ی ۵۰۰ کندلی**:

| اجرا | نرخ نوشتن | تخمین Warmup |
|---|---|---|
| اول | ۳۵٬۱۵۹ کندل/ثانیه | ۸ ثانیه |
| دوم | ۱۸۰٬۲۴۵ کندل/ثانیه | ۲ ثانیه |

**۵ برابر اختلاف.** نه به این دلیل که ماشین پنج برابر شده، بلکه به این دلیل که
۵۰۰ کندل آن‌قدر کوچک است که بیشترش سربارِ فراخوانی و هر کاری که ترمینال در آن
لحظه مشغولش بوده را اندازه می‌گیرد، نه نوشتن را.

و بعد آن عدد را در **۵۷۶** ضرب می‌کردم تا بگویم Warmup چقدر طول می‌کشد.

> قانونی که خودم اول پروژه گذاشتم: «هیچ ادعای Performance بدون Measurement».
> ضرب کردن در ۵۷۶، ادعاست — نه اندازه‌گیری. ابزاری که قرار بود جلوی این را
> بگیرد، خودش مرتکبش می‌شد.

بخش ۵ جدید Preflight حالا:

* در **مقیاس واقعی** می‌نویسد (پیش‌فرض ۵۰٬۰۰۰ کندل در هر پاس، قابل تنظیم)
* **سه پاس** می‌زند، هر پاس روی بازه‌ی زمانیِ جدا و رو به جلو — یعنی همان شکلی
  که Warmup واقعاً می‌نویسد، داخل سری‌ای که هر بار بلندتر شده
* **کندترین پاس** را نقل می‌کند، نه بهترین را. کندترین همانی است که کاربر
  بالاخره پایش می‌نشیند.
* اگر عدد نهایی هنوز فراتر از چیزی است که اندازه گرفته، **خودش می‌گوید چند برابر
  دارد کش می‌آید** و اسمش را می‌گذارد تخمین، نه اندازه‌گیری.

و یک عدد دوم که تا حالا اصلاً اندازه نگرفته بودم: **هزینه‌ی نوشتن تک‌کندلی**.
Warmup به‌صورت انبوه می‌نویسد ولی Replayِ در حال اجرا کندل‌به‌کندل اضافه می‌کند.
این دو نرخ یکی نیستند، و نقل‌کردنِ اولی به‌جای دومی گمراه‌کننده بود. حالا
Preflight می‌گوید Stream روی M1 حدوداً تا چند برابرِ زمان واقعی اشباع می‌شود.

---

## وضعیت

| | |
|---|---|
| Preflight | ✅ اجرا شد، دو بار، روی دو نماد متفاوت |
| BLOCKER | ۰ |
| باگ‌های پیداشده | ۳ — **هر سه در ابزار QA، هیچ‌کدام در محصول** |
| کامپایل ۳۰ فایل | ⏳ هنوز سبز نشده |
| ۱۵ Test Suite (۱٬۲۰۸ Assertion) | ⏳ هنوز اجرا نشده |
| ۱۵ Spike | ⏳ هنوز اجرا نشده |

---

# اجرای سوم — و باگ چهارم، باز هم در ابزار QA

```
BLOCKER  custom symbol create   error 5304
VERDICT: NO GO
```

خطای **۵۳۰۴** یعنی «نمادی با همین نام از قبل هست». نمادِ `SSRPreflight.SSR9`
از اجرای قبلی مانده بود — همان که آن دفعه با خطای ۵۳۰۶ حذف نشد.

زنجیره‌ی کامل، و اینکه چطور یک باگ باگ بعدی را ساخت:

| اجرا | چه شد |
|---|---|
| دوم | `CustomSymbolDelete` بدون Deselect → **۵۳۰۶** → نماد ماند |
| سوم | نماد هست → `CustomSymbolCreate` → **۵۳۰۴** → NO GO |

یعنی اسکریپت **به‌خاطر آشغالِ خودش** اعلام کرد که محصول قابل اجرا نیست.

## محصول این را از اول درست داشت — برای بار سوم

`CSSRCustomSymbolManager::Create()` دو لایه محافظت دارد که Preflight هیچ‌کدام
را نداشت:

```
//--- leftovers from a previous run must go before we recreate
Destroy();                       // Deselect → مکث → Delete
...
if(!CustomSymbolCreate(...))
  {
   //--- Creation can fail because the symbol is STILL THERE: a
   //--- previous session crashed, or a chart refused to close in
   //--- time. Refusing to start would leave the user stuck until
   //--- they cleaned up by hand. If the leftover is demonstrably
   //--- ours, adopt it and wipe its history instead.
```

اگر ساخت شکست بخورد و نمادِ باقی‌مانده **قابل‌اثبات مالِ ما** باشد، آن را
**Adopt** می‌کند نه اینکه امتناع کند. کاربری که جلسه‌ی قبلش Crash کرده، نباید
تا وقتی دستی پاک نکرده زمین‌گیر بماند.

Preflight حالا دقیقاً همان دو لایه را دارد: اول Deselect و مکث و Delete، و اگر
باز هم نشد، Adopt به‌جای BLOCKER — و به‌صورت LIMIT گزارش می‌کند نه سکوت.

## جمع‌بندی چهار باگ تا اینجا

| # | باگ | کجا |
|---|---|---|
| ۱ | سقف ۲۰٬۰۰۰ کندلی که خودم گذاشته بودم و به‌عنوان یافته گزارش می‌شد | ابزار QA |
| ۲ | حذف نماد بدون Deselect (۵۳۰۶) | ابزار QA |
| ۳ | تخمین Warmup با ضرب ۵۷۶ برابری در یک نمونه‌ی ۵۰۰ کندلی | ابزار QA |
| ۴ | نمادِ باقی‌مانده → ۵۳۰۴ → NO GO | ابزار QA |
| — | **در خودِ محصول** | **هیچ** |

این خوشایند نیست — ابزاری که قرار است محصول را بسنجد، چهار بار خودش خطا داشته.
ولی جهتش سالم است: هر چهار بار، رفتار درست همان چیزی بود که محصول از فاز ۳
به بعد داشت، و ابزار داشت به آن می‌رسید نه برعکس.

## یک خط تازه در خروجی

```
data folder: <مسیر>
```

داشتن بیش از یک نصب متاتریدر عادی است، و کامپایلی که در یک Data Folder بنشیند
در حالی که اسکریپت از Data Folder دیگری اجرا می‌شود، **دقیقاً شبیه کپی‌ای است
که هرگز انجام نشده**. حالا خودِ خروجی می‌گوید از کجا اجرا شده.

---

# اجرای چهارم — اولین GO تمیز، و باگ پنجم در ابزار QA

```
build: v8  2026-08-29  leftover-adopt
data folder: C:\Users\...\Terminal\6F2BC4B36A0BAC4B951FC9ADDEF025F7
OK: 22    LIMITS: 1    BLOCKERS: 0
VERDICT: GO
```

نماد سوم: **XAUUSD@** — digits=2، point=0.01، tick_value=1، tick_size=0.01.
سه نماد، سه پروفایل کاملاً متفاوت، و هیچ‌کدام چیزی در محصول نشکست.

| | US30CASH | US30.U26 | XAUUSD@ |
|---|---|---|---|
| ارزش هر Point برای یک لات | ۰٫۰۱ | ۱۰ | ۱ |
| Tick در ۳ روز | ۲۱۱٬۷۲۲ | ۱۸۶٬۴۴۰ | **۴۴۳٬۸۰۴** |
| M1 موجود در ترمینال | — | — | **۱۰۰٬۱۳۷** |
| عمق سرور | — | — | **۲٬۷۴۴ روز** |

خط `server offers back to 2019.02.24 (2744 calendar days)` اولین باری است که
جواب سؤال واقعی را داریم: **بله، Warmup دویست‌روزه از این بروکر قابل دسترس
است.** آن LIMIT قبلی که می‌گفت «۲۰٬۰۰۰ کندل دارد» هرگز یک واقعیت نبود؛ سقف
درخواست خودم بود.

## باگ پنجم — و باز هم ادعای Performance بدون Measurement

```
pass 1: 50000 bars in 14 ms = 3601008 bars/sec
pass 3: 50000 bars in 11 ms = 4397537 bars/sec
```

**۳٫۶ میلیون کندل در ثانیه.** و همین عدد است که نشان می‌دهد اندازه‌گیری غلط
است، نه اینکه دیسک سریع است:

| نمونه | نرخ |
|---|---|
| ۵۰۰ کندل | ۳۵٬۱۵۹ /ثانیه |
| ۵۰۰ کندل (اجرای بعدی) | ۱۸۰٬۲۴۵ /ثانیه |
| ۵۰٬۰۰۰ کندل | **۳٬۶۰۱٬۰۰۸ /ثانیه** |

نمونه صد برابر شد و نرخ **بیست برابر بالا رفت**. این امضای یک فراخوانی است که
**صف می‌کند و برمی‌گردد**، نه یک نوشتن سریع. من زمانِ *برگشتن تابع* را اندازه
می‌گرفتم، نه زمانِ *انجام کار*.

در نسخه‌ی بعد، ساعت وقتی متوقف می‌شود که آخرین کندلِ نوشته‌شده **قابل خواندن**
باشد — با یک `CopyRates` تک‌کندلی که خودش هزینه‌ای ندارد. آن عدد همانی است که
کاربر واقعاً پایش می‌نشیند.

> این پنجمین باگ ابزار QA است و سومین بار که دقیقاً همان قانونی را نقض کرده‌ام
> که خودم اول پروژه گذاشتم. ظاهراً نوشتنِ قانون آسان است و رعایتش هر بار در یک
> شکل تازه سخت می‌شود.

## دو چیز دیگر که در همین خروجی درست نبودند

**«extrapolates to 0 seconds»** — جواب ۰٫۰۸ ثانیه بود و با `%.0f` شد صفر.
عددی که کاربر می‌خواند «شکست» به نظر می‌رسد نه «سریع». حالا زیر یک ثانیه با
میلی‌ثانیه گزارش می‌شود.

**«M1 history: 20000 bars (14 days)»** درست کنارِ «terminal holds 100137».
دو خط که همدیگر را نقض می‌کنند: ۲۰٬۰۰۰ چیزی بود که *خواستم*، نه چیزی که *هست*.
حالا وقتی ترمینال بیشتر دارد، همان را نقل می‌کند.

## و یک یافته‌ی واقعی، اولین بار از جنس معماری نه باگ

```
single-bar append: 189 us per bar
bulk:                0.28 us per bar
```

**نوشتن کندل‌به‌کندل حدود ۶۷۵ برابر گران‌تر از نوشتن دسته‌ای است.**

این تصمیم فاز ۳ را تأیید می‌کند: `WriteBars()` برای Seed دسته‌ای می‌نویسد و
Stream اصلاً از مسیر کندل نمی‌رود، از Tick می‌رود. آن موقع دلیلش «یک تماس
غول‌پیکر ترمینال را قفل می‌کند» بود؛ حالا عدد دومش را هم داریم.

و یک ادعایی که **نکردم**: خروجی می‌گفت Stream تا ۳۱۷٬۵۸۸ برابر زمان واقعی
اشباع می‌شود. از نظر حسابی درست است و از نظر معنا بی‌فایده — آن سقفِ *نوشتن*
است. Redraw چارت و اندیکاتورهای رویش خیلی زودتر از آن گلوگاه می‌شوند، و این
بخش هیچ‌کدامشان را اندازه نگرفته. حالا خودِ خروجی همین را می‌گوید و اندازه‌گیری
واقعی‌اش را می‌گذارد برای Phase 17.

---

# اجرای کامل Spikeها — ۲۰۲۶.۰۸.۲۹ ساعت ۲۲:۰۰

بروکر: **WM Markets Ltd** · سرور `WMMarkets-Demo` · ترمینال build **6140**
ماشین: ۴ هسته · ۸ گیگ رم · `MAX_BARS = 100,000,000`
نماد مبدأ: `XAUUSD@`

هر ۱۴ Spike اجرا شد. **۵۷ Assertion سبز، ۵ قرمز.**

---

## چهار فرضی که کل محصول رویشان ایستاده — هر چهار تأیید شد

### 🟢 A2 — فقط M1 بنویس، ترمینال بقیه را می‌سازد

۴٬۳۲۰ کندل M1 نوشته شد. ترمینال M5/M15/M30/H1/H4 را خودش ساخت:

| تایم‌فریم | کندل مرجع | کندل ترمینال | اختلاف OHLC |
|---|---|---|---|
| M5 | ۸۶۴ | ۸۶۴ | **۰** |
| M15 | ۲۸۸ | ۲۸۸ | **۰** |
| M30 | ۱۴۴ | ۱۴۴ | **۰** |
| H1 | ۷۲ | ۷۲ | **۰** |
| H4 | ۱۸ | ۱۸ | **۰** |

**صفر اختلاف، در هر پنج تایم‌فریم.** حجم هم دقیق.

و آزمایش نوشتن مستقیم M5: ۸۶۴ کندل M5 فرستادیم، `resulting_M1_bars = 864`.
یعنی ترمینال آن را **به M1 تبدیل کرد**. تأیید نهایی اینکه M1 تنها انبار است.

### 🟢 A3 — نشت آینده: صفر

۱۰۰ نقطه‌ی برش تصادفی، **۲٬۸۰۰ Assertion**، در هر تایم‌فریم و هر مسیر خواندن.

```
leaks_found = 0
```

هزینه‌ی یک بازسازی کامل تاریخچه: **۱٫۳۶ میلی‌ثانیه**.

### 🟢 C4 — ساعت مجانی است

`SYMBOL_TIME` دقیقاً برابر زمان تیک تزریق‌شده — **صفر اختلاف**، در دقت میلی‌ثانیه.
یعنی اندیکاتور و EAی خود کاربر بدون یک خط کد واسط، زمان Replay را درست می‌بیند.

و `TimeCurrent()` همان لحظه `2026.08.30 01:02` بود در حالی که ساعت Replay
`2024.01.08 00:16` بود — قاعده‌ی «هرگز `TimeCurrent` در منطق Replay» اثبات شد.

### 🟢 D4 — «کندل قبلی» می‌تواند یک دکمه‌ی واقعی باشد

| روش | زمان |
|---|---|
| حذف دنباله، ۱ کندل | **۲۷ میلی‌ثانیه** |
| حذف دنباله، ۱۰۰ کندل | ۲۸٫۶ میلی‌ثانیه |
| بازسازی کامل | ۸٬۵۶۰ میلی‌ثانیه |

**۳۱۷ برابر ارزان‌تر.** و ۱۰۰ کندل تقریباً به قیمت ۱ کندل.

### 🟢 D2 — تعویض تایم‌فریم وسط Replay

| عمق | بی‌کار | زیر تزریق تیک |
|---|---|---|
| ۱۰k | ۳۱ms | ۶۹۸ms |
| ۵۰k | ۳۱ms | ۷۷۶ms |
| ۱۰۰k | ۴۷ms | ۷۷۳ms |

زیر یک ثانیه در همه‌ی حالت‌ها.

### 🟢 C3 — کانال IPC

`datetime` بدون تلفات از کانال `double` رد می‌شود تا سال ۲۰۹۹.
**۱٫۵ میلیون نوشتن در ثانیه**، ۱٫۸ میلیون خواندن. صفر خواندن پاره.

---

## ۵ قرمز — و فقط یکی‌شان درباره‌ی محصول است

### 🔴 A1 — `tick_size` و `tick_value` کپی نمی‌شوند (تأیید مستقل)

```
prop_TRADE_TICK_SIZE   FAIL  expected=0.01  actual=0.0
prop_TRADE_TICK_VALUE  FAIL  expected=1.0   actual=0.0
```

از ۱۲ خاصیت، ۱۰ تا کپی می‌شوند. این دو نه.

**این همان باگی است که دو دور پیش در محصول پیدا و رفع شد** — آن موقع از روی
شکست یک تست فهمیدمش. حالا Spike مستقلاً همان را نشان می‌دهد. `CloneMoneyProperties`
دقیقاً برای همین نوشته شد و درست بود.

نکته: `properties_mismatch = 2` با یادداشت «PLAN B: manual property map required».
Plan B همان چیزی است که محصول از قبل اجرا می‌کند.

### ⚪ A1 — سؤال سررسید بی‌جواب ماند

```
origin_start = 0   origin_expiration = 0   clone_inherits_lifetime = 0
```

مبدأ `XAUUSD@` بود که **اصلاً سررسید ندارد**. پس این تست چیزی را که قرار بود
بسنجد نسنجید. برای جواب واقعی باید روی چارت `US30.U26` اجرا شود.

پاک کردن بی‌قید و شرطِ عمر در محصول همچنان درست است — ولی هنوز نمی‌دانم لازم بوده یا نه.

### 🟠 D3 — افت ۲۲٫۸٪ توان عبوری

حافظه پایدار (**منفی ۱٫۸٪** — یعنی اصلاً رشد نکرد). ولی نرخ تزریق افت کرد:

| دقیقه | تیک بر ثانیه |
|---|---|
| ۰٫۲ | ۱۰٬۴۴۴ |
| ۵ | ۹۷۳ |
| ۱۰ | ۶۹۶ |
| ۱۶ | ۵۳۷ |

از ۱۰٬۶۰۰ به ۵۳۷ — **۲۰ برابر کندتر** روی یک میلیون تیک. حافظه ثابت.

اگر واقعی باشد، یعنی `CustomTicksAdd` با رشد تاریخچه‌ی تیک کند می‌شود، و
جلسه‌های طولانی Replay به‌مرور کند می‌شوند. **این مهم‌ترین ورودی فاز ۱۷ است.**

ولی D3 نمی‌سنجید که تیک‌ها **پذیرفته** شده‌اند یا نه. اضافه شد. باید دوباره اجرا شود.

---

# و حالا بخش ناخوشایند — ۴ باگ در ابزار اندازه‌گیری

## 🔴 B2 — ۱۰٫۵ میلیارد تیک در ثانیه

```
batch=100     accepted=200000/200000   3.86s     51,768 tick/s   ← تنها عدد واقعی
batch=500     accepted=0/200000        0.000189s  1,058,201,058 tick/s
batch=1000    accepted=0/200000        0.000106s  1,886,792,452 tick/s
batch=5000    accepted=0/200000        0.000038s  5,263,157,894 tick/s
batch=10000   accepted=0/200000        0.000022s  9,090,909,090 tick/s
batch=50000   accepted=0/200000        0.000019s 10,526,315,789 tick/s
```

هر شش حالت از یک زمان شروع می‌کردند. `CustomTicksAdd` فقط **جلو** اضافه می‌کند،
پس حالت اول محدوده را نوشت و پنج حالت بعدی همان مُهرهای زمانی را دوباره فرستادند
و **کاملاً رد شدند**. یک فراخوانی رد شده در میکروثانیه برمی‌گردد، و اسپایک
تعداد **فرستاده** را بر آن تقسیم کرد.

و بعد نتیجه گرفت:

```
optimal_batch_size = 50000   "lock this into Feeder"
```

**دقیقاً برعکسِ حقیقت.** آن اندازه، آن است که ترمینال هیچ‌کدامش را قبول نکرد.
و هر دو Verdict هم **PASS** دادند، روی عددی که ساختگی بود.

## 🔴 B4 — یک BLOCKER دروغ

```
tick_history_depth          = 0 days   "NO tick history - F1 impossible"
copyticksrange_week_return  = 1,022,369 ticks
```

دو خط پشت سر هم، و یکی دیگری را تکذیب می‌کند.

کاوشگر عمق، یک پنجره‌ی **یک‌ساعته** نمونه می‌گرفت و اولین ساعتِ خالی را
«پایان تاریخچه» می‌فهمید. امروز **شنبه** است. یک روز قبل، بازار بسته بود.

**یک ساعت خالی یعنی بازار بسته، نه تاریخچه‌ی غایب.** حالا یک **روز** کامل
نمونه می‌گیرد و تا سه روز خالی پشت سر هم صبر می‌کند.

اگر این را باور کرده بودم، F1 را از نقشه‌ی راه حذف می‌کردم و Import از CSV را
جلو می‌انداختم — بر اساس یک باگ.

## 🔴 D1 — ۵٫۵ میلیون کندل در ثانیه

```
write_time =    17.9 ms
sync_wait  = 9,161   ms
total_time =     9.18 s
bars_per_sec = 5,588,465    ← تقسیم بر write_time
```

عدد واقعی: ۱۰۰٬۰۰۰ کندل در ۹٫۱۸ ثانیه = **حدود ۱۰٬۹۰۰ کندل بر ثانیه**.
**۵۰۰ برابر** خوش‌بینانه‌تر گزارش شده بود.

این همان اشتباهی است که **یک بار در Preflight اصلاح کردم** و در D1 باقی مانده بود.

### و یک الگوی عجیب در همان اعداد

```
seed_100k_chunk1000    sync_wait =    28 ms
seed_100k_chunk5000    sync_wait = 5,855 ms
seed_100k_chunk10000   sync_wait = 9,271 ms
seed_50k_chunk10000    sync_wait = 9,278 ms
seed_10k_chunk10000    sync_wait = 9,300 ms   ← ده هزار کندل، همان ۹٫۳ ثانیه
```

**ثابت است، مستقل از حجم داده.** ۱۰٬۰۰۰ کندل همان‌قدر طول می‌کشد که ۱۰۰٬۰۰۰.

تنها چیزی که بین حالت اول و بقیه فرق دارد: حالت‌های بعدی نمادی را حذف و با
**همان نام** دوباره می‌سازند. اگر این علت باشد، محصول هم دقیقاً همین کار را
بین جلسه‌ها می‌کند و یک هزینه‌ی ثابت ۹ ثانیه‌ای می‌پردازد.

**نتیجه نمی‌گیرم.** یک متریک اضافه شد (`symbol_recreated`) تا الگو در داده باشد
نه در برداشت من.

## 🟠 B1 — تیک فوری است، سری نه

```
reflect_symbolinfotick = 20 / 20   ← تیک بلافاصله دیده می‌شود
reflect_iclose         =  1 / 20   ← سری کندل، نه
```

`SymbolInfoTick` هر ۲۰ تیک را دید. `iClose(...,0)` فقط یکی.

اسپایک **بدون هیچ انتظاری** می‌خواند. سری کندل را ترمینال با زمان‌بندی خودش
بازسازی می‌کند. این «کندل از تیک ساخته نمی‌شود» نیست؛ «چند میلی‌ثانیه بعد
ساخته می‌شود» است.

همان درسی که در `T3.6` گرفتیم: **نوشتن، قبل از اینکه خواندن معتبر شود، برمی‌گردد.**

حالا صبر می‌کند و **مدت را اندازه می‌گیرد** — یک تأخیر، یک نتیجه است؛ یک شکستِ خالی نه.

---

## اصلاح ریشه‌ای، نه چهار وصله

هر سه باگ B2 و D1 و D3 یک بیماری‌اند: **نرخی که از کاری که انجام نشده حساب می‌شود.**

پس در `SSR_SpikeKit` یک نگهبان اضافه شد:

```
double SSR_Rate(const long sent, const long accepted, const double seconds)
  {
   if(sent <= 0 || accepted < sent) return -1.0;   // نه یک نرخ
   ...
```

از این به بعد هر عدد توان عبوری از اینجا رد می‌شود. اگر کاری که ادعا شده
انجام نشده باشد، خروجی `NOT MEASURED` است، نه یک رکورد سرعت.

**شمارش فاز ۱۶ تا اینجا:** ۹ باگ در ابزار QA، ۴ باگ واقعی در محصول، ۷ تست غلط یا کهنه.
