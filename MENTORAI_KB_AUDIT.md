# ممیزی اولیه‌ی پروژه — پایگاه دانش MENTORAI

**تاریخ:** ۲۰۲۶-۰۹-۰۵ · **نسخه:** v001 · **مخزن:** `mehdifx21hoseini-debug/academyss` · **شاخه:** `claude/mentorai-knowledge-base-setup-w2pfq3`

این سند نتیجه‌ی بازرسی کامل مخزن پیش از هرگونه پردازش انبوه است و به‌عمد در همان تاریخ تثبیت شده است.
> اعداد بخش ۵ مربوط به همان لحظه‌ی ممیزی‌اند؛ وضعیت و شمارش جاری پروژه در `MENTORAI_KB_PROGRESS.md` نگهداری می‌شود.

---

## ۱. مخزن در وضعیت فعلی چیست؟

مخزن **وب‌سایت آکادمی سبحان صمدی** است، نه یک پروژه‌ی پایگاه دانش. کل محتوای اولیه:

| فایل | حجم | محتوا |
|---|---|---|
| `index.html` | ۱۴.۵KB | صفحه‌ی اصلی: معرفی، پادکست‌ها، کتاب‌ها، منتورینگ، نظرات، تماس |
| `admin.html` | ۱۲.۷KB | پنل مدیریت محتوا |
| `js/data.js` | ۵.۵KB | داده‌ی محتوا: ۶ پادکست، ۶ کتاب صوتی، ۶ نظر کاربر |
| `js/player.js` | ۸.۵KB | موتور پخش صوتی |
| `js/app.js` · `js/admin.js` | ۱۷.۳KB | منطق صفحه و پنل مدیریت |
| `css/*` | ۳۶.۴KB | استایل |
| `assets/*` | ۲ فایل | لوگو و عکس |

**نتیجه‌ی کلیدی:** هیچ منبع دانشی (ترنسکریپت دوره، جزوه، دیتاست پرسش‌وپاسخ، مستندات بروکر) در مخزن وجود ندارد.

---

## ۲. پوشش منابع دانش

| منبع | وضعیت | توضیح |
|---|---|---|
| دوره‌ی مقدماتی | ❌ موجود نیست | `RQ-0001` |
| دوره‌ی پیشرفته | ❌ موجود نیست | `RQ-0001` |
| دوره‌ی روان‌شناسی | ❌ موجود نیست | `RQ-0003` |
| سند متدولوژی آکادمی | ❌ موجود نیست | `RQ-0008` — بدون آن، مرز «آکادمی» و «دانش عمومی» قابل اجرا نیست |
| قواعد و FAQ آکادمی | ❌ موجود نیست | — |
| کاتالوگ و خدمات آکادمی | ⚠️ ناقص | از `js/data.js` و `index.html` استخراج شد؛ نیازمند تأیید (`RQ-0005`) |
| پرسش و پاسخ منتور | ❌ موجود نیست | `RQ-0002` — مهم‌ترین دارایی غایب |
| متاتریدر ۴ و ۵ | ⚠️ نمونه | نوشته‌شده آفلاین، تأییدنشده (`RQ-0007`) |
| بروکر | ❌ موجود نیست | `RQ-0004` |
| دانش عمومی معامله‌گری | ⚠️ نمونه | ۲۲ اصطلاح پایه، تأییدنشده |
| روان‌شناسی معامله‌گری | ⚠️ نمونه | ۱۰ الگو، به‌عنوان جایگزین موقت |

---

## ۳. یافته‌های مهم

**F1 — هیچ محتوای آموزشی آکادمی در دسترس نیست.** بدون آن، دانش `ACADEMY_PRIMARY` صفر است و
مربی هوش مصنوعی نمی‌تواند به سؤال‌های روشی پاسخ دهد. این گلوگاه اصلی پروژه است.

**F2 — متدولوژی رسمی آکادمی مستند نشده است.** عنوان محتوای سایت به «پرایس اکشن» و «تحلیل تکنیکال»
اشاره دارد، اما هیچ سندی نمی‌گوید چه چیزی جزو روش آکادمی است و چه چیزی نیست. قاعده‌ی «حفاظت از
متدولوژی» بدون این سند فقط تا حد قرنطینه‌ی روش‌های بیرونی قابل اجراست.

**F3 — تعارض در حجم محتوای منتشرشده (`CONF-0001`).** سایت «+۸۰ ساعت محتوا» اعلام می‌کند، اما
هیچ‌کدام از ۱۲ آیتم صوتی فایل صوتی ندارند (`src` خالی است). تا روشن شدن موضوع، مربی نباید وجود یا
قابل‌پخش بودن این محتواها را تأیید کند.

**F4 — احتمال نمونه بودن داده‌های سایت.** آمار بازدید (۱۲.۳K، ۸.۷K …) و مدت‌زمان‌ها گرد و
تأییدنشده‌اند و کتاب‌های صوتی عمدتاً کتاب‌های ناشران خارجی‌اند. همه‌ی رکوردهای کاتالوگ
`PENDING_REVIEW` ثبت شدند و هیچ‌کدام به‌عنوان واقعیت قطعی وارد پایگاه دانش نشدند.

**F5 — محیط پردازش دسترسی اینترنت ندارد.** واکشی مستندات رسمی متاکوتس با خطای egress مسدود شد
(`www.metatrader5.com` → 403 از پراکسی). بنابراین کل دانش متاتریدر با `source_type = MODEL_DRAFT`
و `verification_required = true` ثبت شده است و هیچ رکوردی `APPROVED` نیست.

**F6 — نظرات کاربران وارد پایگاه دانش نشدند.** محتوای تبلیغاتی است، نه دانش؛ ورود آن‌ها فقط
نویز بازیابی تولید می‌کرد.

**F7 (خارج از محدوده‌ی این پروژه، اما قابل توجه) — امنیت پنل مدیریت.** نام کاربری و رمز عبور پیش‌فرض
پنل مدیریت هم در `README.md` و هم به‌صورت ثابت در `js/admin.js` قرار دارد و احراز هویت کاملاً
سمت مرورگر است؛ یعنی هر بازدیدکننده می‌تواند آن را ببیند و دور بزند. این موضوع به پروژه‌ی سایت/CORE
مربوط است و در این پروژه تغییری روی آن اعمال نشد. رمز عبور عمداً در هیچ فایل پایگاه دانش ثبت نشده است.

---

## ۴. تصمیم‌های مهندسی که مستقل گرفته شد

| تصمیم | دلیل |
|---|---|
| ساختار `raw_sources → knowledge_base → exports` | تضمین ردیابی‌پذیری و عدم تخریب منبع اصلی |
| هر فایل JSON یک «مجموعه» با پاکت متادیتا | نسخه‌بندی و شمارش دقیق در سطح مجموعه |
| شناسه‌ی پایدار `<دامنه>-<نوع>-<شماره>` | ارجاع‌دهی متقابل و پایداری در نسخه‌های بعدی |
| هفت سطح مرجعیت با `ACADEMY_PRIMARY` در رأس | اجرای عملی قاعده‌ی «آکادمی مرجع نهایی است» |
| `MODEL_DRAFT` برای هر محتوای بدون منبع بیرونی | جلوگیری ساختاری از تبدیل حدس به دانش رسمی |
| قرنطینه‌ی روش‌های بیرونی در لایه‌ی جدا | جلوگیری از آلودگی متدولوژی آکادمی |
| ابزار پایتون بدون وابستگی | اجرای تکرارپذیر در هر محیط، حتی بدون اینترنت |
| اعتبارسنج با قواعد ثابت (invariant) | خطاهای مرجعیت و منبع، به‌جای بازبینی دستی، خودکار گرفته می‌شوند |

---

## ۵. آنچه در این مرحله ساخته شد

* اسکیمای کامل: `schemas/` (شیء دانش، پرسش‌وپاسخ منتور، تعارض، صف بازبینی، مجموعه، واژگان کنترل‌شده)
* خط پردازش تکرارپذیر: `tools/kb_run.sh` (استخراج → ساخت → اعتبارسنجی → مانیفست → خروجی)
* ۹۴ شیء دانش در ۱۱ مجموعه (کاتالوگ آکادمی، واژه‌نامه، متاتریدر ۴ و ۵، عیب‌یابی، مقایسه، روان‌شناسی، قرنطینه، قواعد، تعارض، صف بازبینی)
* خروجی کامل Markdown + JSON + CSV در `exports/`
* مانیفست اصلی: `manifests/knowledge_manifest_v001.json`
* گزارش اعتبارسنجی: `reports/validation_report_v001.json` (۰ خطا، ۰ هشدار)

---

## ۶. اولویت کارهای بعدی

| اولویت | کار | پیش‌نیاز |
|---|---|---|
| P0 | پردازش دوره‌ی مقدماتی | دریافت ترنسکریپت (`RQ-0001`) |
| P0 | استخراج پیش‌نویس متدولوژی آکادمی | همان بالا (`RQ-0008`) |
| P0 | خط پردازش پرسش‌وپاسخ منتور روی نمونه‌ی ۱۰۰ گفت‌وگویی | دریافت دیتاست (`RQ-0002`) |
| P1 | تأیید یا حذف رکوردهای کاتالوگ سایت | پاسخ آکادمی (`RQ-0005`) |
| P1 | ~~تکمیل دانش متاتریدر~~ (انجام شد) و تأیید آن | راهنمای آفلاین یا بازبینی منتور (`RQ-0007`) |
| P1 | ساخت لایه‌ی بروکر | نام بروکر و مستندات (`RQ-0004`) |
| P2 | جایگزینی لایه‌ی روان‌شناسی با محتوای آکادمی | دوره‌ی روان‌شناسی (`RQ-0003`) |
| P2 | افزودن جست‌وجوی معنایی و شاخص بازیابی برای پروژه‌ی CORE | تثبیت ساختار فعلی |
