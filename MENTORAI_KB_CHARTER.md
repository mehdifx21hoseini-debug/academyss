# منشور پایگاه دانش MENTORAI

این سند، قواعد حاکم بر پروژه‌ی «پایگاه دانش MENTORAI» است و مرجع رسمی تصمیم‌های
دانش‌مهندسی در این مخزن به شمار می‌رود. رکوردهای سیاست‌گذاری در پایگاه دانش
(`MET-RULE-*`) به همین فایل ارجاع می‌دهند.

## ۱. تفکیک دو پروژه

| پروژه | مسئولیت |
|---|---|
| MENTORAI CORE | اجرای ایجنت، حافظه، مکالمه، رفتار پاسخ، ارجاع به انسان، ارکستراسیون بازیابی |
| MENTORAI KNOWLEDGE BASE (این مخزن) | ساخت، پاک‌سازی، ساختاردهی، اعتبارسنجی، نسخه‌بندی و خروجی‌گیری دانش |

این پروژه منطق ایجنت را تغییر نمی‌دهد؛ فقط دانشِ تمیز، ساختاریافته و نسخه‌دار تولید می‌کند.

## ۲. زنجیره‌ی تغییرناپذیر داده

```
raw_sources/  →  cleaned  →  knowledge_base/ (structured)  →  validated  →  exports/
```

* فایل خام هرگز بازنویسی یا حذف نمی‌شود.
* هر شیء دانش باید از طریق `source.source_ref` و `source.source_location` تا منبع اصلی ردیابی شود.
* خروجی‌ها نسخه‌دار هستند و روی نسخه‌ی قبلی بازنویسی نمی‌شوند.

## ۳. مرجعیت (Authority)

ترتیب اعتبار، از بالا به پایین:

1. `ACADEMY_PRIMARY` — محتوای رسمی آموزشی آکادمی (ترنسکریپت دوره، سند رسمی).
2. `ACADEMY_DERIVED` — اطلاعات عملیاتی و کاتالوگ آکادمی (سایت، اپلیکیشن).
3. `MENTOR_VERIFIED` — پاسخ منتور که با محتوای رسمی تطبیق داده شده است.
4. `VENDOR_OFFICIAL` / `BROKER_OFFICIAL` — مستندات رسمی متاکوتس یا بروکر.
5. `MENTOR_UNVERIFIED` — پاسخ منتور که هنوز بررسی نشده است.
6. `GENERAL_KNOWLEDGE` — دانش عمومی معامله‌گری.
7. `EXTERNAL_METHODOLOGY` — روش‌های بیرونی، فقط در قرنطینه.

**هیچ منبع بیرونی نمی‌تواند متدولوژی آکادمی را بازنویسی کند.** در صورت تعارض،
هر دو طرف حفظ می‌شوند و یک `CONFLICT_RECORD` ساخته می‌شود.

## ۴. حفاظت از متدولوژی آکادمی

روش‌های بیرونی (Smart Money Concepts، ICT، ایچیموکو، امواج الیوت، وایکاف، هارمونیک،
اردر فلو، ولوم پروفایل و مانند آن‌ها) فقط برای «شناخت و توضیح کوتاه» و با برچسب
`EXTERNAL_METHODOLOGY_QUARANTINE` نگهداری می‌شوند و هرگز به متدولوژی آکادمی وارد نمی‌شوند.

## ۵. ممنوعیت دانش ساختگی

* هیچ قاعده‌ای صرفاً به دلیل «منطقی به نظر رسیدن» ساخته نمی‌شود.
* اطلاعات ناقص با دانش عمومی تکمیل نمی‌شود.
* در صورت تردید: `approval_status = REVIEW_REQUIRED` و ثبت در `knowledge_base/review_queue/`.
* هر محتوایی که بدون منبع بیرونی نوشته شده باشد `source_type = MODEL_DRAFT` و
  `verification_required = true` می‌گیرد و هرگز `APPROVED` نمی‌شود.

## ۶. تفکیک دامنه‌ها

دانش آکادمی، دانش عمومی معامله‌گری، دانش متاتریدر، دانش بروکر، دانش روان‌شناسی و
حافظه‌ی اختصاصی دانشجو شش چیز جدا هستند. حافظه‌ی دانشجو در این مخزن ذخیره نمی‌شود؛
مسئولیت آن با پروژه‌ی CORE است. داده‌های شناسایی‌کننده‌ی دانشجو پیش از خروج از
`raw_sources/` حذف می‌شوند (`pii_removed = true`).

## ۷. نسخه‌بندی دانش

وضعیت‌های چرخه‌ی عمر: `ACTIVE`، `SUPERSEDED`، `DEPRECATED`، `PENDING_REVIEW`، `CONFLICTING`.
دانش قدیمی حذف نمی‌شود؛ با `supersedes` / `superseded_by` به نسخه‌ی جدید متصل می‌شود.

## ۸. تعریف موفقیت

پایگاه دانش زمانی موفق است که پاسخ‌های بازیابی‌شده «درست، دارای منبع، سازگار با آکادمی،
قابل ردیابی، نسخه‌دار و اعتبارسنجی‌شده» باشند و مرز میان متدولوژی آکادمی و سایر لایه‌های
دانش هرگز مخدوش نشود.
