-- افزونه‌ها هنگام ساخت پایگاه داده و با کاربر ابرکاربر نصب می‌شوند.
-- مهاجرت‌ها عمداً این کار را نمی‌کنند، چون نقش برنامه نباید ابرکاربر باشد.
CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
